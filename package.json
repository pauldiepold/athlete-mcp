{
  "name": "athlete-mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run && pnpm --dir web run test",
    "test:watch": "vitest",
    "probe:koerperdaten": "tsx scripts/probe-koerperdaten.ts",
    "backfill:koerperdaten": "tsx scripts/backfill-koerperdaten.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "tsx": "^4.22.4",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "wrangler": "^4.19.0"
  }
}
